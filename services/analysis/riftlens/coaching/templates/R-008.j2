{% if certainty == 'certain' %}You missed{% else %}It looks like you missed{% endif %} {{ monster }} at {{ t_mmss }} (~{{ distance }} units from the pit) without a plate/tower trade (CS delta {{ cs_delta }}). Distance uses interpolated frames.
