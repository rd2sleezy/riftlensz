Flash is on your modelled loadout and was never cast (match-total casts 0). This death recap spanned {{ span_s }}s, so it was not an instant burst. Summoner timing is not available from timeline events alone.
