{% if certainty == 'certain' %}You held{% else %}You appear to have held{% endif %} about {{ gold }} unspent gold for {{ hold_s }}s (threshold {{ threshold }}). Gold between minute frames is reconstructed without timeline goldPerSecond.
