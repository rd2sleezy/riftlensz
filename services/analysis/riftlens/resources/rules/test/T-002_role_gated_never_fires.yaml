# Throwaway H.6 engine test rule — jungle-only, so laners never match.
id: T-002
version: 1
name: "Role-gated jungle-only (test)"
concept_id: MACRO.OBJECTIVES.DRAGON_SETUP
category: MACRO
severity_base: MEDIUM
data_tier: RIOT_ONLY
patch_range: null

applicability:
  roles: [JUNGLE]
  champions:
    include: ["*"]
    exclude: []
  champion_tags_exclude: []
  queues: [420, 400, 440, 430]
  phases: [EARLY, MID, LATE]
  min_rank: null
  max_rank: null

required_inputs:
  - facts: [LEVEL_UP]
  - features: []
  - min_confidence: {}

trigger:
  evaluate_on: EVENT
  fact_kinds: [LEVEL_UP]
  predicate: rules.test.role_gated_never_fires

params: {}

severity_modifiers: []

evidence_template:
  - "Would fire only for jungle subjects"

explanation: |
  Test rule: applicability restricts evaluation to JUNGLE.

better_alternative: |
  Not a coaching suggestion.

false_positives:
  - "Never fires for non-jungle roles by design"
  - "Not intended for production reviews"

suppressed_by: []
cooldown_ms: 0
max_findings_per_match: 8
