# Throwaway H.6 suppression-pair member — stronger finding that hides T-003.
id: T-004
version: 1
name: "Strong finding (test, suppresses T-003)"
concept_id: RISK.DEATH_CAUSE.DEATH_IN_A_1VN
category: RISK
severity_base: HIGH
data_tier: RIOT_ONLY
patch_range: null

applicability:
  roles: [TOP, JUNGLE, MIDDLE, BOTTOM, UTILITY]
  champions:
    include: ["*"]
    exclude: []
  champion_tags_exclude: []
  queues: [420, 400, 440, 430]
  phases: [EARLY, MID, LATE]
  min_rank: null
  max_rank: null

required_inputs:
  - facts: [LEVEL_UP]
  - features: []
  - min_confidence: {}

trigger:
  evaluate_on: EVENT
  fact_kinds: [LEVEL_UP]
  predicate: rules.test.strong_finding

params: {}

severity_modifiers: []

evidence_template:
  - "Strong test signal at {t_ms}"

explanation: |
  Stronger test finding. Suppresses T-003 at the same timestamp.

better_alternative: |
  Not a coaching suggestion.

false_positives:
  - "Always fires on level-up by design"
  - "Suppression pair is synthetic"

suppressed_by: []
cooldown_ms: 0
max_findings_per_match: 8
