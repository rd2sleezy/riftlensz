# Throwaway H.6 engine test rule — not a production coaching rule.
id: T-001
version: 1
name: "Always fires on level-up (test)"
concept_id: LANING.LEVEL_SPIKES.LEVEL_2_PRIORITY_RACE
category: LANING
severity_base: LOW
data_tier: RIOT_ONLY
patch_range: ">=12.1"

applicability:
  roles: [TOP, JUNGLE, MIDDLE, BOTTOM, UTILITY]
  champions:
    include: ["*"]
    exclude: []
  champion_tags_exclude: []
  queues: [420, 400, 440, 430]
  phases: [EARLY, MID, LATE]
  min_rank: null
  max_rank: null

required_inputs:
  - facts: [LEVEL_UP]
  - features: []
  - min_confidence: {}

trigger:
  evaluate_on: EVENT
  fact_kinds: [LEVEL_UP]
  predicate: rules.test.always_fires

params: {}

severity_modifiers: []

evidence_template:
  - "Level-up at {t_ms} for pid {pid}"

explanation: |
  Test rule: fires on every LEVEL_UP fact for the subject.

better_alternative: |
  Not a coaching suggestion.

false_positives:
  - "Fires on every level-up by design"
  - "Not intended for production reviews"

suppressed_by: []
cooldown_ms: 0
max_findings_per_match: 8
